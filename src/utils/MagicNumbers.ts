// =============================================================
// Centralized magic numbers shared across multiple files.
// =============================================================

/** Number of semitones in one octave (chromatic scale). */
export const SEMITONES_PER_OCTAVE = 12;

/** Perfect fifth interval in semitones. */
export const PERFECT_FIFTH_SEMITONES = 7;

/** Default/active/home note circle radii (Spiral, Icosahedron). */
export const NOTE_CIRCLE_RADIUS_DEFAULT = 2.5;
export const NOTE_CIRCLE_RADIUS_ACTIVE = 5;
export const NOTE_CIRCLE_RADIUS_HOME = 3;
